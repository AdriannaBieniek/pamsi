#pragma once
namespace Merging
{
    template <typename T>
    void merging(T *&array, int start, int middle, int end);
    template <typename T>
    void merge_sort(T *&array, int start, int end);
}
#include "merging.cpp"
